query GetChats($limit: Int!, $cursor: String) {
  getChats {
    id
    createdById
    groupName
    groupAvatarUrl
    messages(limit: $limit, cursor: $cursor) {
      ...MessageFragment
    }
    members {
      id
      firstName
      lastName
      username
      profilePictureUrl
    }
  }
}
